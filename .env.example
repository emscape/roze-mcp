# API Base URLs for dev (emulator) and prod environments
API_BASE_DEV=http://127.0.0.1:5001/PROJECT_ID/us-central1
API_BASE_PROD=https://REGION-PROJECT_ID.cloudfunctions.net

# Proxy mode: 'dev-only' (default) or 'all'
# dev-only: Only allows proxy calls to dev environment (recommended)
# all: Allows proxy calls to both dev and prod (not recommended)
PROXY_MODE=dev-only

# Log level for debugging
LOG_LEVEL=info
